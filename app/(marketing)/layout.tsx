/**
 * Public pages render PublicFrame themselves via SitePage.
 * This layout stays empty so PillNav and HelixFooter are not stacked on top.
 */
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return children
}
